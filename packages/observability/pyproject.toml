[project]
name = "switchboard-observability"
version = "0.1.0"
description = "Structured log helper. Owner: ATLAS interface, SENTINEL hardens redaction."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["switchboard_observability"]
